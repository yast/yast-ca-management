/**
 * $Id$
 * Summary:
 *   SCR Agent for executing openssl commands
 * Authors:
 *   Michael Calmer <mc@suse.de>
 * Access:
 *   execute
 * See:
 *   
 *
 *
 * Example:
 *   Execute (.openssl.genKey, map options)
 */
.openssl
`ag_openssl (
  `OPENSSL ($[
    "bin"       : "/usr/bin/openssl",
    "tmpdir"    : "/tmp",
    "stderr"    : "/dev/null",
    "debug"     : "0",
    "privkeyalg": "des3",
  ])
)
